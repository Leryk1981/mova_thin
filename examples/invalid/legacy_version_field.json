{
  "version": "3.2",
  "id": "legacy_version_test",
  "description": "Test plan with legacy version field instead of mova_version",
  "actions": [
    {
      "type": "print",
      "params": {
        "value": "This should fail - using legacy version field"
      }
    }
  ]
}
